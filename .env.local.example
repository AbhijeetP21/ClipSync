# Supabase credentials — copy this file to .env.local and fill in real values.
# Find these in your Supabase project under Settings → API.

# Project URL, e.g. https://abcdefgh.supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co

# Browser key (safe to expose). Supabase's new "publishable" key (sb_publishable_...).
# Legacy projects may still use NEXT_PUBLIC_SUPABASE_ANON_KEY instead — the code accepts either.
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx

# service_role key — NOT currently used by the app code, but listed here for
# parity with the README in case you add server-side admin operations.
# Keep this secret; never expose it to the browser.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
